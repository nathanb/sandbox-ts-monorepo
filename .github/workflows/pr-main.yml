name: Push to main with lib-ui changes

on:
  pull_request:
    branches: [ main ]

jobs:
  run-tests:
    if: ${{ !contains(github.event.head_commit.message, '#skip') }}
    uses: ./.github/workflows/test-all.yaml
    secrets: inherit
